namespace SynappsTest.Endpoints;

public class FakeService
{
    public void Post()
    {
        // not an HTTP endpoint — no base type, no Configure(), no Map()
    }

    public void Get()
    {
        // not an HTTP endpoint — no base type, no Configure(), no Map()
    }
}
